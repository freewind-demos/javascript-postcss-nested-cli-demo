{
  "scripts": {
    "demo": "postcss hello.pcss --use autoprefixer --use postcss-nested -o ./build/hello.css"
  },
  "devDependencies": {
    "autoprefixer": "^9.1.5",
    "postcss-cli": "^6.0.0",
    "postcss-nested": "4.1.2"
  }
}
